[
  {
    "op": "replace",
    "location": "Up-Front Questionnaire table, gate_margin row Notes column",
    "content": "candidate must beat the best by at least this margin",
    "rationale": "FOURTH attempt at terminology anchoring. Previous: iter-01 loop.md table (no effect), iter-02 Quick Reference (no effect), iter-03 HTML comment (no effect). This time: REPLACE the first-encounter definition entirely so 'beat the best' is the ONLY concise description. Theory: subagent's concise-answer generation pulls from the FIRST definitional encounter, not supplementary sections."
  },
  {
    "op": "add",
    "location": "After 'Run Directory Layout' section, before 'Resume'",
    "content": "## ledger.py CLI Reference\n\n```\nledger.py record --run <path> --iter <N> --version <label> --split train|holdout --scores <comma-separated-floats>\nledger.py gate   --run <path> --iter <N> --candidate <cN> --margin <float>\nledger.py best   --run <path>\n```\n\n`--scores` takes comma-separated floats, e.g. `--scores \"0.8,1.0,0.6\"`.",
    "rationale": "Proven useful for exec_ledger train tasks."
  },
  {
    "op": "add",
    "location": "Minibatch Sampling section, after last bullet",
    "content": "Epoch count after K iterations = K×M/T (float division; partial epochs count).",
    "rationale": "Proven useful for epoch calculation tasks."
  }
]
