iter1: accept c1 -> v1 (holdout 0.90 -> 1.00); two edits:
  1. Reworded frozen-target discipline bullet: "receives only {skill text, task}; no contamination, no self-grading" → "receives exactly two inputs: the current skill text and the task prompt. Nothing else is passed to the subagent."
  2. Added plain-string note after questionnaire table: "Values above are plain strings (e.g. autonomous, not code-fenced)."
